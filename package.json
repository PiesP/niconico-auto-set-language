{
  "name": "niconico-auto-set-language",
  "version": "0.10.0",
  "description": "Automatically set language to Japanese on NicoNico",
  "license": "MIT",
  "author": "PiesP",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PiesP/niconico-auto-set-language.git"
  },
  "homepage": "https://github.com/PiesP/niconico-auto-set-language#readme",
  "bugs": {
    "url": "https://github.com/PiesP/niconico-auto-set-language/issues"
  },
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.2.2",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.2.2"
  },
  "scripts": {
    "clean": "rimraf dist",
    "build:ts": "tsc --project tsconfig.build.json && node --experimental-strip-types scripts/bundle-userscript.ts dist/niconico-auto-set-language.user.js",
    "build:min": "terser dist/niconico-auto-set-language.user.js -o dist/niconico-auto-set-language.user.min.js -c directives=false -m --comments '/^\\s*==UserScript==|^\\s*@|^\\s*==\\/UserScript==/' --source-map",
    "build:dev": "pnpm run clean && pnpm run build:ts",
    "typecheck": "pnpm -s check",
    "check": "tsc --noEmit --project tsconfig.build.json",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "fmt:check": "pnpm -s fmt",
    "fmt": "biome check --linter-enabled=false .",
    "fmt:fix": "biome check --write --linter-enabled=false .",
    "knip": "knip --dependencies",
    "check-versions": "node --experimental-strip-types scripts/check-versions.ts",
    "quality": "pnpm -s fmt && pnpm -s lint && pnpm -s check && pnpm -s knip",
    "quality:fix": "pnpm -s fmt:fix && pnpm -s lint:fix && pnpm -s check && pnpm -s knip",
    "quality:ci": "pnpm -s quality",
    "dev": "tsc --watch --project tsconfig.build.json",
    "prebuild": "pnpm run check-versions && pnpm run quality",
    "build": "pnpm run clean && pnpm run build:ts",
    "verify": "pnpm run build && pnpm run build:min"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/tampermonkey": "^5.0.5",
    "knip": "^6.18.0",
    "rimraf": "^6.1.3",
    "terser": "^5.48.0",
    "typescript": "^6.0.3"
  },
  "volta": {
    "node": "26.3.1",
    "pnpm": "11.2.2"
  }
}
