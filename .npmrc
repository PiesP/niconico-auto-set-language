# pnpm Configuration (xcom 스타일)

# Enforce compatible Node/pnpm versions
engine-strict=true

# Lockfile format
lockfile-version=3

# Install behavior
prefer-frozen-lockfile=true
auto-install-peers=true
strict-peer-dependencies=false

# Performance
prefer-offline=true
