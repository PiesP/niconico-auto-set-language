name: "🚀 Release"

# Responsibility: Publish tagged userscript releases
# - Build release assets from the tagged commit
# - Sync userscript metadata to the release version and CDN URLs
# - Publish files to the release branch and GitHub Releases
#
# Trigger: Tag push (v*)

'on':
  push:
    tags:
      - "v*"

env:
  NODE_VERSION: "24"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: "🚀 Publish Release"
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: 🛡️ Guard unsupported pnpm/action-setup majors
        run: |
          set -euo pipefail
          if grep -REn 'uses:[[:space:]]*pnpm/action-setup@v([5-9]|[1-9][0-9]+)' .github/workflows; then
            echo "pnpm/action-setup major >=5 is blocked until the pnpm 10 install regression is fixed upstream." >&2
            exit 1
          fi

      # pnpm/action-setup@v6 can route pnpm 10 projects through a pnpm 11
      # launcher on GitHub Actions, which breaks install with
      # ERR_PNPM_BROKEN_LOCKFILE. Keep v4 until that upstream regression is fixed.
      # Do not also set `with.version` here: the repository-wide `packageManager`
      # field is the single source of truth for the pnpm 10 version we expect.
      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"

      - name: 🔎 Verify pnpm available
        run: pnpm -v

      - name: 📝 Determine version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          if [[ -z "$VERSION" ]]; then
            echo "Error: derived version is empty" >&2
            exit 1
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "RELEASE_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=v$VERSION" >> "$GITHUB_ENV"

      - name: 📦 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build userscript
        run: pnpm build

      - name: 🔧 Sync userscript @version to tag
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          version = os.environ["RELEASE_VERSION"]
          p = Path("dist/niconico-auto-set-language.user.js")
          text = p.read_text(encoding="utf-8")

          pat = re.compile(r"^//\s*@version\s+.*$", re.MULTILINE)
          repl = f"// @version      {version}"
          new_text, n = pat.subn(repl, text, count=1)
          if n != 1:
            raise SystemExit(f"Expected to replace exactly 1 @version line, replaced {n}")

          p.write_text(new_text, encoding="utf-8")
          print(f"Synced @version -> {version}")
          PY

      - name: 🔗 Sync userscript updateURL/downloadURL
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import os
          import re
          from pathlib import Path

          def main() -> None:
            repo = os.environ.get("GITHUB_REPOSITORY", "")
            if not repo:
              raise SystemExit("GITHUB_REPOSITORY is missing")

            url = f"https://github.com/{repo}/releases/latest/download/niconico-auto-set-language.user.js"

            p = Path("dist/niconico-auto-set-language.user.js")
            text = p.read_text(encoding="utf-8")

            def upsert(key: str) -> None:
              nonlocal text
              pat = re.compile(rf"^//\\s*@{re.escape(key)}\\s+.*$", re.MULTILINE)
              repl = f"// @{key}  {url}"
              if pat.search(text):
                text = pat.sub(repl, text, count=1)
                return
              for anchor in ("@match", "@description", "@version"):
                a = re.search(rf"^//\\s*{re.escape(anchor)}.*$", text, re.MULTILINE)
                if a:
                  idx = a.end()
                  text = text[:idx] + "\n" + repl + text[idx:]
                  return
              text = text.replace("// ==UserScript==", "// ==UserScript==\n" + repl, 1)

            upsert("downloadURL")
            upsert("updateURL")
            p.write_text(text, encoding="utf-8")
            print("Synced @downloadURL/@updateURL ->", url)

          main()
          PY

      - name: 🗜️ Build minified userscript
        run: pnpm build:min

      - name: 🧪 Verify UserScript metadata
        run: |
          set -euo pipefail
          if ! head -n 1 dist/niconico-auto-set-language.user.js | grep -q "// ==UserScript=="; then
            echo "Error: UserScript metadata is missing from the build output"
            exit 1
          fi
          if ! grep -q "^// @version[[:space:]]\+${RELEASE_VERSION}$" dist/niconico-auto-set-language.user.js; then
            echo "Error: @version does not match tag-derived version (${RELEASE_VERSION})"
            grep -n "^// @version" dist/niconico-auto-set-language.user.js || true
            exit 1
          fi
          if ! grep -q "^// @downloadURL" dist/niconico-auto-set-language.user.js; then
            echo "Error: @downloadURL missing"
            exit 1
          fi
          if ! grep -q "^// @updateURL" dist/niconico-auto-set-language.user.js; then
            echo "Error: @updateURL missing"
            exit 1
          fi
          if [ ! -f dist/niconico-auto-set-language.user.min.js ]; then
            echo "Error: Minified build output is missing"
            exit 1
          fi

      - name: 📤 Publish to release branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: release
          publish_dir: ./dist
          force_orphan: true

      - name: 🏷️ Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: |
            dist/niconico-auto-set-language.user.js
            dist/niconico-auto-set-language.user.min.js
            dist/niconico-auto-set-language.user.min.js.map
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 📊 Upload release artifacts
        uses: actions/upload-artifact@v7
        with:
          name: release-v${{ steps.version.outputs.version }}
          path: dist/
          if-no-files-found: error
          retention-days: 30
